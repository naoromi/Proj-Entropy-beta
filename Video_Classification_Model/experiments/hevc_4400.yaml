---
mode: train  # 3 modes: train, eval, inference

gpus: [0, 1]

paths:
  base: /mnt/hbnas/home/fgan/Proj-Entropy/Video_Classification_Model
  
  # logic: for each model, run each dataset in every archive
  #        run metadata should be stored in serialized pytorch state dictionary
  model:    resnet
  archive:  penguin_archive
  dataset:  hevc_4400
  
  # run specific configs (input videos path is separate b/c often external)
  input_videos: /mnt/hbnas/home/fgan/Proj-Entropy/Video_Classification_Model/1_input_videos/hevc_4400
  

...

